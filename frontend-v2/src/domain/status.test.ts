import { calculateRunProgress, deriveRoomRuntime, npcStatusFromActivity } from './status';
import type { Activity,Room,Task } from './types';
const baseActivity=(patch:Partial<Activity>={}):Activity=>({id:1,agentId:7,teamId:1,roomId:2,type:'llm_infer',status:'started',title:'',detail:'',startedAt:'2026-01-01T00:00:00Z',finishedAt:null,metadata:{},...patch});
const room:Room={id:2,teamId:1,name:'经世阁',i18n:{},type:'group',state:'SCHEDULING',needScheduling:true,currentTurnAgentId:7,agentIds:[7,8],tags:[]};let n=0;const task=(status:string):Task=>({id:++n,teamId:1,roomId:2,assigneeId:7,title:'任务',description:'',status,priority:'NORMAL',result:'',updatedAt:null});
describe('status mapping',()=>{it('maps inference and retries',()=>{expect(npcStatusFromActivity(baseActivity())).toBe('thinking');expect(npcStatusFromActivity(baseActivity({metadata:{retry_attempt:2}}))).toBe('retrying')});it('derives completed room from tasks',()=>{const result=deriveRoomRuntime({...room,needScheduling:false,state:'IDLE'},[task('DONE'),task('DONE')],[]);expect(result.status).toBe('completed');expect(result.progress).toBe(100)});it('derives discussing room and speaker',()=>{const result=deriveRoomRuntime(room,[task('IN_PROGRESS')],[baseActivity()],()=> '诸葛先生');expect(result.status).toBe('discussing');expect(result.currentNpcStatus).toBe('thinking');expect(result.activityLabel).toContain('诸葛先生')});it('progress is phase aware and monotonic',()=>{const rooms=[deriveRoomRuntime({...room,needScheduling:false,state:'IDLE'},[task('DONE')],[])];expect(calculateRunProgress(rooms,'discussing')).toBe(64);expect(calculateRunProgress(rooms,'synthesizing')).toBeGreaterThan(64);expect(calculateRunProgress(rooms,'completed')).toBe(100)})});
