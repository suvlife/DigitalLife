# Fonts

The application imports pinned `@fontsource/lxgw-wenkai@5.2.5`. Vite self-hosts
its WOFF2 files, compatible with `font-src 'self'`. Never fetch fonts from an
unreviewed URL. Preserve `licenses/LXGW-WEN-KAI-NOTICE.md` and package `OFL.txt`.
