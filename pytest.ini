[pytest]
asyncio_mode = auto
python_files = test.py test_*.py
xfail_strict = true
filterwarnings =
    ignore::pytest.PytestUnhandledThreadExceptionWarning:tests.integration.test_chat_flow.test
addopts =
    --strict-markers
markers =
    forked: run this test item in a dedicated subprocess
